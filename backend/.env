PORT=4000
DATABASE_URL=postgresql://pulse:pulse@127.0.0.1:5432/pulse
ENCRYPTION_KEY=replace-me-with-32-byte-base64-key
ALLOWED_ORIGIN=http://localhost:5173
ALLOWED_EMAIL_DOMAIN=kiit.ac.in
SESSION_SECRET=pulse-dev-session-secret-change-me

# OTP email delivery (Gmail SMTP via App Password).
# Generate an App Password at https://myaccount.google.com/apppasswords
# — regular account passwords won't work with SMTP and 2FA must be on.
# Both must be set; if either is missing, OTPs are only logged to the
# server console and no email is sent.
EMAIL_USER= abhijaijaiss@gmail.com
EMAIL_APP_PASSWORD= xufw seko eiwi bsfb
